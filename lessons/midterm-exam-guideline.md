---
description: รายละเอียดเนื้อหาการสอบกลางภาค ()
icon: lightbulb-exclamation-on
---

# Midterm Exam Guideline

## 📘 รายละเอียดเนื้อหาการสอบกลางภาค (Midterm Exam)

**วิชาการเขียนโปรแกรมเชิงวัตถุ (Object Oriented Programming)** 🎯 **คะแนนเต็ม:** 100 คะแนน | 🕒 **เวลา:** 3 ชั่วโมง

***

### 📊 ตารางสรุปเนื้อหาและจุดประสงค์การเรียนรู้ (Learning Outcomes)

<table><thead><tr><th>หัวข้อหลัก (Topics)</th><th width="363.40008544921875">รายละเอียดสิ่งที่ต้องรู้ (Learning Outcomes)</th><th align="center">รูปแบบข้อสอบ</th><th align="center">คะแนน</th></tr></thead><tbody><tr><td><strong>1. แนวคิดเชิงวัตถุ</strong><br>(OOP Concepts)</td><td>• เข้าใจความหมายของ <strong>Class</strong> และ <strong>Object</strong> และความแตกต่าง<br>• เข้าใจหลักการ 4 เสาหลัก:<br>  - <code>Encapsulation</code> (การห่อหุ้ม)<br>  - <code>Abstraction</code> (การลดรูป)<br>  - <em>Inheritance &#x26; Polymorphism</em> (เน้น 2 ตัวแรก)<br>• เข้าใจหน้าที่ของ Access Modifiers (<code>private</code>, <code>public</code>)</td><td align="center">ปรนัย<br>(กากบาท)</td><td align="center"><strong>30</strong></td></tr><tr><td><strong>2. 1 โครงสร้างคลาสและไวยากรณ์ Java</strong><br>(Class Anatomy &#x26; Syntax)</td><td>• <strong>การประกาศตัวแปร</strong> (Attribute/Field): การเลือกชนิดข้อมูล (<code>int</code>, <code>String</code>, <code>double</code>)<br>• <strong>การสร้างเมธอด</strong> (Method): การเขียน Signature, Return Type และ Parameter<br>• <strong>การสร้างวัตถุ</strong> (Instantiation): การใช้คำสั่ง <code>new</code> และการประกาศ Reference</td><td align="center">เติมคำในช่องว่าง<br>(เขียนโค้ด)</td><td align="center"><strong>20</strong></td></tr><tr><td><strong>2.2. การจัดการสถานะของวัตถุ</strong><br>(Object State Manipulation)</td><td>• <strong>Getter/Setter</strong>: การเขียนเมธอดเพื่อดึงค่า หรือกำหนดค่าให้ Attribute ที่เป็น <code>private</code><br>• <strong>Logic ภายในเมธอด</strong>: การเขียนโค้ดเพื่อเปลี่ยนแปลงค่า (เช่น ฝากเงินแล้ว <code>balance</code> เพิ่ม)</td><td align="center">เขียนโปรแกรม<br>(Coding)</td><td align="center"><strong>10</strong></td></tr><tr><td><strong>3. อัลกอริทึมพื้นฐานบนอาร์เรย์</strong><br>(Array Logic &#x26; Algorithms)</td><td>• <strong>Looping</strong>: การใช้ <code>for</code> เพื่อเข้าถึงสมาชิกใน Array<br>• <strong>Counting</strong>: การใช้ <code>if</code> เพื่อนับจำนวนสมาชิกตามเงื่อนไข<br>• <strong>Accumulation</strong>: การหาผลรวม (Sum) ทั้งหมด หรือเฉพาะช่วง<br>• <strong>Comparison</strong>: การเปรียบเทียบค่าระหว่าง 2 Arrays ในตำแหน่งเดียวกัน</td><td align="center">เขียนโปรแกรม<br>(Coding)</td><td align="center"><strong>40</strong></td></tr></tbody></table>

{% hint style="info" %}
ข้อเขียนโปรแกรม ไม่จำเป็นต้องสร้าง main method เพื่อแสดงการทดสอบโปรแกรม ให้เขียนตามที่โจทย์กำหนดก็พอ
{% endhint %}

***

&#x20;





***

#### 📘 แนวทางการเตรียมตัวเขียนโปรแกรม&#x20;

ในส่วนนี้จะเน้นการทำงานกับ Array และ Logic การตัดสินใจ (Decision Making)   ให้ซ้อมจากโจทย์ที่ใช้โครงสร้างเดียวกับข้อสอบจริง&#x20;

{% hint style="warning" %}
ไม่ใช่ข้อสอบจริงๆ อย่าท่องโค้ดไปสอบนะ&#x20;
{% endhint %}

***

<mark style="background-color:blue;">**1. รูปแบบ: การนับจำนวนตามเงื่อนไข (Counting Logic)**</mark>

Concept: วนลูปเช็คของทุกชิ้นในตะกร้า (Array) ถ้าชิ้นไหนหน้าตาเหมือนที่โจทย์สั่ง ให้กดเครื่องนับจำนวนเพิ่มทีละ 1

* โจทย์ซ้อม: จงเขียนคลาส `Counter` และเมธอด `countPositives` ที่รับ Array เข้ามา แล้วนับว่ามี "เลขจำนวนเต็มบวก" (ค่ามากกว่า 0) กี่ตัว
*   โครงสร้างโค้ดที่ควรฝึก:

    ```java
    public int countPositives(int[] arr) {
        int count = 0; // 1. เริ่มต้นตัวนับเป็น 0
        for (int i = 0; i < arr.length; i++) {
            if (arr[i] > 0) { // 2. เงื่อนไขที่โจทย์ต้องการ (ในข้อสอบอาจเปลี่ยนเป็น เท่ากับ, น้อยกว่า)
                count++;      // 3. เจอแล้วให้นับเพิ่ม
            }
        }
        return count; // 4. ส่งผลรวมกลับ
    }
    ```
* จุดสังเกต: โจทย์ข้อสอบจริงอาจจะเปลี่ยนเงื่อนไขจาก `> 0` เป็นเงื่อนไขอื่นๆ ขอให้แม่นเรื่องเครื่องหมายเปรียบเทียบ



โจทย์: จงเขียนคลาส `ScoreCounter` และเมธอด `countFail` เพื่อรับอาร์เรย์คะแนนเข้ามา และนับว่ามีกี่คนที่ได้คะแนน "น้อยกว่าเกณฑ์ที่กำหนด"

รูปแบบที่ 1: เขียนเป็น Method (สำหรับทำข้อสอบ Part 3) _เน้นให้นักศึกษาฝึกเขียน return type และ parameter_

```java
public class ScoreCounter {

    // โจทย์กำหนดให้รับ array และ เกณฑ์คะแนน (threshold) เข้ามา
    public int countFail(int[] scores, int threshold) {
        int count = 0;

        // ใช้ for-each loop: วนลูปดึงคะแนนออกมาทีละตัว (score) จากถัง (scores)
        for (int score : scores) {
            
            // เช็คเงื่อนไข: ถ้าน้อยกว่าเกณฑ์ ให้ยิ้มนิ้วนับ
            if (score < threshold) {
                count += 1; // หรือใช้ count++; ก็ได้
            }
        }
        
        return count; // ส่งผลลัพธ์จำนวนคนที่นับได้กลับไป
    }
}
```

***

รูปแบบที่ 2: การนำไปทดสอบ (แบบที่อาจารย์ยกตัวอย่าง) _ส่วนนี้ช่วยให้เข้าใจ Logic การไหลของโปรแกรม_

```java
public class TestLogic {
    public static void main(String[] args) {
        // 1. สร้างชุดข้อมูลตัวอย่าง
        int[] scores = {85, 90, 78, 92, 88, 55, 63, 96};
        int target = 90;

        // 2. เตรียมตัวแปรสำหรับนับ
        int cnt = 0;

        // 3. เริ่มวนลูป
        for (int score : scores) {
            // เช็คว่าคะแนนตัวปัจจุบัน น้อยกว่า 90 หรือไม่?
            if(score < target){
                cnt += 1;  // ถ้าใช่ ให้นับเพิ่ม
            }
        }

        // 4. แสดงผลลัพธ์
        System.out.println("สมาชิกที่ค่าน้อยกว่า " + target + " มีจำนวน: " + cnt + " คน");
    }
}
```

#### 📝 จุดสังเกต :

1. For-each Loop: `for (int score : scores)` อ่านง่ายกว่า `for (int i=0; i<scores.length; i++)` แต่ใช้ได้ดีเมื่อเราต้องการแค่ "ค่าข้อมูล" ไม่ได้ต้องการ "ตำแหน่ง (index)" &#x20;
2. ตัวแปรสะสมค่า:  ต้องประกาศ `int cnt = 0;` ไว้นอกลูปเสมอ ถ้าไปประกาศในลูป ค่าจะถูกรีเซ็ตเป็น 0 ทุกรอบ
3. Return vs Print: <kbd><mark style="color:yellow;background-color:$primary;">**ถ้า**<mark style="color:yellow;background-color:$primary;"></kbd><mark style="color:$primary;">ในข้อสอบ โจทย์สั่งให้ คืนค่า</mark> (`return`) ห้ามเผลอเขียน `System.out.println` ในเมธอดนับจำนวนเด็ดขาด (เว้นแต่โจทย์สั่งให้แสดงผล)

***

<mark style="background-color:blue;">**2. รูปแบบ: การเปรียบเทียบ 2 Arrays (Parallel Array Processing)**</mark>

Concept: มีแถว 2 แถวที่ยาวเท่ากัน เราต้องหยิบตัวที่ตำแหน่งเดียวกัน (index `i`) มาเทียบกัน แล้วสร้างผลลัพธ์ใส่แถวใหม่

ตัวอย่างทางเลือก: ระบบตรวจสอบสต็อกสินค้า (Inventory Check) เปลี่ยนจาก `score1, score2` เป็น `stock` (ของที่มี) กับ `order` (ยอดสั่งซื้อ)

* โจทย์: รับ `int[] stock` และ `int[] order`
* Logic:
  * ถ้าสินค้าพอขาย (`stock[i] >= order[i]`) ให้เก็บจำนวนสินค้าที่เหลือ (`stock - order`)
  * ถ้าสินค้าไม่พอ (`else`) ให้เก็บค่า `-1` (แจ้งเตือน)

โค้ดตัวอย่าง:

```java
public int[] checkStock(int[] stock, int[] order) {
    // 1. สร้าง Array ผลลัพธ์ (ขนาดยังเท่าเดิม)
    int[] remaining = new int[stock.length];

    for (int i = 0; i < stock.length; i++) {
        // 2. เปลี่ยนเงื่อนไขเป็นเรื่อง "พอ/ไม่พอ" (>=)
        if (stock[i] >= order[i]) {
            remaining[i] = stock[i] - order[i]; // ตัดสต็อก
        } else {
            remaining[i] = -1; // ของขาด
        }
    }
    return remaining;
}
```

&#x20;

***

**3. รูปแบบ: ผลรวมในช่วงข้อมูล (Range Summation)**

&#x20;— ยังทำไม่เสร็จ หิวข้าวก่อน --

***

#### <mark style="color:orange;">📝 Checklist ก่อนส่งโค้ด</mark>

1. วงเล็บปีกกาครบไหม? (เปิด `{` แล้วต้องปิด `}`)
2. Semi-colon มีหรือยัง? (จบท้ายคำสั่งด้วย `;`)
3. Array Index: วนลูปใช้ `i < arr.length` (ไม่มีเท่ากับ) และเรียกใช้ด้วย `arr[i]`
4. Return Type: หัวข้อสอบบอกให้คืนค่าแบบไหน (`int` หรือ `int[]`) บรรทัด `return` ต้องส่งค่าแบบนั้นกลับไป

